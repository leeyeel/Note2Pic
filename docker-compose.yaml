services:
  app:
    build: .
    image: note2pic-mcp:v1.0.2
    container_name: note2pic-mcp
    environment:
      PORT: "${PORT:-7810}"
      PUBLIC_BASE: "${PUBLIC_BASE:-http://host.docker.internal:${PORT:-7810}}"
      TITLE_LINE1_FONT_SIZE: "${TITLE_LINE1_FONT_SIZE:-120}"
      # Leave unset to use automatic body sizing. Set PAGE_FONT_SIZE to lock it.
      PAGE_FONT_SIZE: "${PAGE_FONT_SIZE:-}"

      
    ports:
      - "${PORT:-7810}:${PORT:-7810}"
    volumes:
      - ./output:/workspace/output
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
